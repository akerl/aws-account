item do
  name 'dns::terraform'

  expected do
    github_release
    repo 'hashicorp/terraform'
    regex /v([\d.]+)/
  end

  actual do
    grep
    file 'circle.yml'
    regex /^\s+TERRAFORM_VERSION: ([\d.]+)$/
  end
end
